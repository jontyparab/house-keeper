export default {
    firstName(state) { return state.firstName },
    lastName(state) { return state.lastName },
    phone(state) { return state.phone },
    city(state) { return state.city },
    address(state) { return state.address },
    isWorker(state) { return state.isWorker },
}